Create a new virtual environment inside the directory
(where 'env' is a new directory which will be created under current directory)

# Python 3
$ python3 -m venv env

In order to use this environment¡¦s packages/resources in isolation, 
you need to ¡§activate¡¨it. 

$ source env/bin/activate

go back to the ¡§system¡¨ context by executing deactivate:

(env) $ deactivate

There is no command for deleting your virtual environment. 
Simply deactivate it and rid your application of its artifacts by recursively removing it.